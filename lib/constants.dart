import 'package:flutter/material.dart';

// Cores utilizadas no aplicativo
const kPrimaryColor = Color(0xFF0C9869);  // Cor primária
const kTextColor = Color(0xFF3C4046);  // Cor do texto
const kBackgroundColor = Color(0xFFF9F8FD);  // Cor de fundo

const double kDefaultPadding = 20.0;  // Preenchimento padrão
